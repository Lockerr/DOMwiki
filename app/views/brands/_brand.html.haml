.alert.span3.alert-info.brand{style: 'margin-bottom: 5px; width: 200px', id: brand.id}
  %button.close{type: :button, data:{dismiss: :alert}}
    &times;
  = brand.name
  %input{id: "#{object}_brand_ids_#{brand.id}", name: "#{object}[brand_ids][]", value: brand.id, type: :hidden}
 
