.alert.alert-info{style: 'margin-bottom: 5px;'}
  %button.close{type: :button, data:{dismiss: :alert}}
    &times;
  = item.name
  %input{id: "#{response.controller_name}_itemmm_ids_#{item.id}", name: "offer[item_ids][]", value: item.id, type: :hidden}
 
